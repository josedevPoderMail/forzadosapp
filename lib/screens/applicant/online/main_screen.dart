import 'package:flutter/material.dart';

class MainScreenOn extends StatelessWidget {
  const MainScreenOn({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}